import React from 'react';

// Minimal mock for lucide-react icons used in tests
export const ShieldCheck = (props) => React.createElement('span', { 'data-icon': 'ShieldCheck', ...props });

export default {};


